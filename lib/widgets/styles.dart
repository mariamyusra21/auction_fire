import 'package:flutter/material.dart';

class BidStyle {
  // a static function so without creating object it can call the class 
  static const boldStyle= TextStyle(
     fontSize: 24, fontWeight: FontWeight.bold, color: Colors.black,
    
  );
}